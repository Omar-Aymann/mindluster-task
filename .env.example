# API Configuration
# Set this to override the default API URL detection
# Leave empty to use automatic detection (localhost:3001 for local, /api/tasks for production)
VITE_API_URL=

# Example for local development:
# VITE_API_URL=http://localhost:3001/tasks

# Example for production (if using external API):
# VITE_API_URL=https://api.example.com/tasks
